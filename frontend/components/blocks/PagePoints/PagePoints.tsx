import styled from 'styled-components';

const PagePointsWrapper = styled.section``;

const PagePoints = () => {
	return (
		<PagePointsWrapper>
			PagePoints
		</PagePointsWrapper>
	);
};

export default PagePoints;
