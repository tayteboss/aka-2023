import styled from 'styled-components';

const PageFooterWrapper = styled.section``;

const PageFooter = () => {
	return (
		<PageFooterWrapper>
			PageFooter
		</PageFooterWrapper>
	);
};

export default PageFooter;
