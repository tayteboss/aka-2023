import styled from 'styled-components';

const PageSummaryWrapper = styled.div``;

const PageSummary = () => {
	return (
		<PageSummaryWrapper>
			PageSummary
		</PageSummaryWrapper>
	);
};

export default PageSummary;
